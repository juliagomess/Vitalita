import { StyleSheet } from "react-native";
export default styles = StyleSheet.create({
  Title: {
    fontSize: 40,
    color: "orange",
    fontWeight: "bold",
  },
  titleContainer: {
    alignItems: "center",
    justifyContent: "center",
    padding: 60,
  },
});
