import { StyleSheet } from "react-native";
export default styles = StyleSheet.create({
  Container: {
    justifyContent: "center",
    alignItems: "center",
    minHeight: "70%",
  },
  title: {
    fontSize: 30,
    fontWeight: "bold",
  },
  subTitulo: {
    fontSize: 16,
  },
});
